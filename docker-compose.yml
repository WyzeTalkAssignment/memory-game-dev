version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://thatodon_db_user:zeR6HTcCSu8Ou5dl@cluster0.12ajxnp.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0/memory-game
    depends_on:
      - mongo
    restart: unless-stopped
    healthcheck:
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s